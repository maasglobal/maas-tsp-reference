{
  "tspAdapterCreateRequestTemplate": {
    "application/json": {
      "leg": "$input.json('$.leg')",
      "meta": "$input.json('$.meta')",
      "customer": "$input.json('$.customer')"
    }
  },
  "environment": {}
}
